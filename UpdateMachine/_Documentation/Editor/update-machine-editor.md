# Muffin Tools Docs - `UpdateMachineEditor`

Custom editor for `UpdateMachine` component.

## Methods

```cs
public override void OnInspectorGUI()
```

Called when an `UpdateMachine` component settigns are drawn in the inspector.